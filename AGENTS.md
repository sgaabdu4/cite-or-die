# Agent Rules

- Hosted pseudonymization failures must be reproduced against the retrieved chunks that caused the UI error. Add a regression for the exact generic source-language pattern, keep the real-name rejection tests passing, and verify the active `/chat/stream` or `CiteOrDieService.chat` path before closing.
